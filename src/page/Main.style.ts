import styled from '@emotion/styled';

export const SWrapper = styled('div')(() => ({
    width: '1480px',
    margin: '0 auto',
    marginTop: '40px',
    display: 'flex',
    flexDirection: 'column',
}));
