import styled from '@emotion/styled';

export const SNav = styled('div')(() => ({
    height: '30px',
    display: 'flex',
    flexDirection: 'row',
    backgroundColor: '#4478FF',
    borderRadius: '30px 30px 0 0',
    border: '1px solid #4478FF',
    padding: '10px 10px',
}));

export const SValidation = styled('div')(() => ({
    width: '20%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    padding: '0 10px',
    color: '#FFFFFF',
    fontFamily: 'Inter',
    fontStyle: 'normal',
    fontWeight: '500',
}));
