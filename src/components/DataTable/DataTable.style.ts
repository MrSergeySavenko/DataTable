import styled from '@emotion/styled';

export const SDataTableWrapper = styled('div')(() => ({
    display: 'flex',
    flexDirection: 'column',
    border: '1px solid #4478FF',
    borderRadius: '30px 30px 0 0',
}));

export const SDataTableArr = styled('div')(() => ({
    display: 'flex',
    flexDirection: 'row',
}));
